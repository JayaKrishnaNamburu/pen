---
"@input/pen-react": patch
---

Wire the AX3 caret-anchored popup contract onto the React slash menu.

Listbox/option IDs, aria-activedescendant, and field aria-controls/aria-expanded stay on the existing selectedIndex model so the editing field keeps DOM focus.
