---
"@input/pen-conformance": patch
---

Add the Node authorityCompare trace replay. Recorded structural traces and DOM↔authority observations keep matched, mismatch, and unchecked distinct — only matched counts as a hold.
