---
"@input/pen-preset-default": patch
---

Declare the default preset's batteries in the README — extensions, schema set, and options — and pin that list to the constructed extension stack so the declaration cannot drift (HB4).
