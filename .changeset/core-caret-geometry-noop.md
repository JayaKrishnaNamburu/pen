---
"@input/pen-core": minor
---

Make `pen.caretUp` / `pen.caretDown` a handled diagnostic no-op when geometry is missing mid-block, and read collapse through selection helpers instead of stamped properties.
