---
"@input/pen-types": minor
"@input/pen-core": minor
"@input/pen-vue": minor
---

Accept a deep-readonly selection shape on the selection helpers so every caller can read without a live document walk. Vue now uses those helpers instead of computed selection properties.
