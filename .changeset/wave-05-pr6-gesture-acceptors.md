---
"@input/pen-dom": patch
---

Accept document-select-all field leftover only inside an open gesture window.

Closed-window Firefox/WebKit collapse still diverges without requesting P2, so projection does not loop. The three v1 suppress flags stay deleted. Table-cell arrows remain sequenced around I4 by the contenteditable cell early-return.
