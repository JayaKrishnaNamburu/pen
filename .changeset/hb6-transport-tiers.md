---
"@input/pen-transport-direct": patch
"@input/pen-transport-sse": patch
---

Declare each transport's HB6 support status in its README and cover the host-driven `PenTransport.stream()` path with one integration test per package.
