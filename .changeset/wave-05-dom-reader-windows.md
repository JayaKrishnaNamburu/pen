---
"@input/pen-dom": patch
---

Accept DOM selection reads only inside an open gesture window.

Closed-window leftover ranges request re-projection and do not write the authority. Same-ids block proposals without a live `head` keep the authority head. The leftover-on-other-block stamp still protects the Enter-split session switch.
