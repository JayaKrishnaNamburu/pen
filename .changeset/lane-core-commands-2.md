---
"@input/pen-core": patch
---

Register `pen.caretUp` / `pen.caretDown` with goal-column persistence (cleared on non-vertical motion), pin the inline-atom select-vs-delete fork, and add a headless default-keymap catalog (I6).
