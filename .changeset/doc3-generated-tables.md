---
"@input/pen-docs": patch
---

Generate the docs site message-key and diagnostic-code tables from the default catalog and diagnostic emit sites so a drift fails the docs build instead of a reader noticing it.
