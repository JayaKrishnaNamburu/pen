---
"@input/pen-dom": patch
"@input/pen-react": patch
"@input/pen-vue": patch
---

Resolve block content-host `dir` through core's four-step direction chain instead of reading `props.direction` alone.
