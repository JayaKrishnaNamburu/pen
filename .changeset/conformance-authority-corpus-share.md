---
"@input/pen-conformance": patch
---

Share the authority-trace corpus and hash from one module. Both compare entry points import it, so a new case cannot pass on one side while the other stays on a stale recording.
