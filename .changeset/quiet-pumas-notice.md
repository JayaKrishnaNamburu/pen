---
"@input/pen-multiplayer": patch
---

Treat awareness as untrusted input.

Remote presence is validated on read for shape, length, rate, and the local document so a hostile peer cannot break rendering or inject markup through cursor attributes.
