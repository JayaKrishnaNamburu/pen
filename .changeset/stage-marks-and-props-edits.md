---
"@input/pen-ai": patch
---

Stage `format-text` and `set-props` as reviewable suggestions under suggestion mode instead of writing them durably. Accept applies the stored marks or props; reject leaves the document as it was before the turn (`spec/packages/extensions/ai.md` EC11).
