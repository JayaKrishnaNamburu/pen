---
"@input/pen-core": patch
"@input/pen-dom": patch
---

Keep a mixed-boundary pointer drag as a text range from the mid-paragraph start onto a fully covered divider so Backspace keeps the prefix and deletes the divider.
