---
"@input/pen-dom": patch
---

Add the unwired Wave 05 selection reader snap rule and gesture-window table.

`isLogicallyEquivalent` is the E2 echo filter: a DOM read that snaps to the same type and points as the authority is a no-op. The gesture-window table is a pure open/close function so keyboard cannot be mistaken for user-intent. Nothing imports this file yet.
