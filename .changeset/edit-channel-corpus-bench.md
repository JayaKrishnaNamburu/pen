---
"@input/pen-ai": patch
---

Add the Wave 0 GATE 0.14 corpus bench: a headless double-driven run of both edit channels, a `scripts/edit-channel-bench.mjs --both --corpus` entry that writes `spec-better-ai/evidence/wave-0-corpus.md`, and per-prompt metrics including wrong-edits and time to first feedback. Scripted doubles prove the harness, not contract adherence.
