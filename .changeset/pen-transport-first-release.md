---
"@input/pen-transport": minor
---

First public release. Transports for Pen: an in-process `./direct` transport for hosts that run generation alongside the editor, and a `./sse` Server-Sent Events transport.
