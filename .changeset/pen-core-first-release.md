---
"@input/pen-core": minor
---

First public release. The headless, extension-first editor engine for human-AI co-authoring: the `editor.apply(ops, { origin })` mutation pipeline, validation, normalization, selection, the extension manager, and the event surface. Runs without a DOM via `createHeadlessEditor`.
