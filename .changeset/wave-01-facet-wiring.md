---
"@input/pen-core": patch
"@input/pen-types": patch
---

Add `editor.facet()` and `editor.whenReady()`, wire v1 contributions behind the initial core facets, adapt `getSlot`/`setSlot` over mapped controller facets with a one-shot `slot-deprecated` diagnostic, and give production writers `internals.assignSlot`.
