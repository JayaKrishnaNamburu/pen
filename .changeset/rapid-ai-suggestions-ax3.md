---
"@input/pen-react": patch
---

Wire the AX3 detached-surface keyboard contract onto the React AI suggestions popover.

The review card is a `role="dialog"` labeled from the active suggestion. Escape closes it and restores focus to the editing position.
