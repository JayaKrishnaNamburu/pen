---
"@input/pen-dom": patch
---

Assert the beforeinput-map, follower-cache, empty-block caret, and bidi-run geometry scenarios on Chromium, WebKit, and Firefox.

The four repairs themselves are recorded in the matching pen-dom changesets; this one is the three-engine matrix, not a second copy of those fixes.
