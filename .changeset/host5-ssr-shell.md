---
"@input/pen-react": patch
---

Route React layout effects through an SSR-safe seam.

Server rendering is shell-only: the editor container hydrates without document content, and hosts that need HTML export from their own copy with `@input/pen-export-html`.
