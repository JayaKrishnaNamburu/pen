---
"@input/pen-core": patch
"@input/pen-dom": patch
"@input/pen-react": patch
---

Match suggestion-menu triggers in the logical offset domain (inline atoms count as length 1) and expose `removeInlineAtom` plus renderer `interaction.remove`.
