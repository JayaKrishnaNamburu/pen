---
---

Give the seven static gates root npm aliases and run the ones that can actually fail on every pull request.

`ch-gates`, `skip-hygiene`, and `renderer-inventory --strict` block. `doc-refs` reports until its remaining README samples type-check. The three leftover inventories that always exit 0 are aliased so the documented commands exist, not turned into permanently green checks.
