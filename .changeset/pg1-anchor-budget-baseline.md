---
"@input/pen-bench": patch
---

Record the PG1 anchor-budget baseline and compare counts so a missing file or a no-op encodeCount fails by name. Absolute µs/ms mint/resolve budgets stay record-only (CH8).
