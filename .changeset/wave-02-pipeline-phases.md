---
"@input/pen-core": patch
"@input/pen-types": patch
---

Name the eight commit-pipeline phases, emit `apply-storm` when nested applies exceed queue depth 16, and expose `internals.onPipelinePhase` for the Wave 2.1 boundary test. `change` / `documentCommit` remain one-release adapters beside `editor.on("commit")`.
