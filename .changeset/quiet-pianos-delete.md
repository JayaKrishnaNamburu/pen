---
"@input/pen-core": minor
---

Remove the inert `toZod` stub from the public surface.

The export always threw "Not implemented"; absence is the honest API (API10) until a real implementation exists.
