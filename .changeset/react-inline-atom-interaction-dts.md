---
"@input/pen-react": patch
---

Keep `InlineAtomRenderInteractionProps` imported in the published `@input/pen-react` declarations so `InlineAtomRenderProps.interaction` type-checks with `skipLibCheck: false`.
