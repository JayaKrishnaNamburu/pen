---
"@input/pen-dom": patch
---

Cover the M2 keymap swap and the M5/M6 no-swap regressions headlessly from mixed-direction fixtures, and record that live `editor.selection` now carries `affinity` after command writes.
