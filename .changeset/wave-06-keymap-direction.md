---
"@input/pen-dom": patch
---

Remap left/right and word caret commands after keymap match when the caller reports an rtl focus block. The key string itself is left alone so other bindings on that key still match.
