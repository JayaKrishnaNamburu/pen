---
"@input/pen-core": patch
"@input/pen-types": patch
---

Release authority anchors on document swap, run AN14 remote repair when structural is empty, and stop reminting stream heads outside the repair step.

`Assoc` is declared only in `changes.ts` and re-exported from `anchors.ts`.
