---
"@input/pen-bench": patch
---

Require a post-clock named count on every published bench so a silent no-op fails by name, and keep BENCHMARK_METADATA in lockstep with the suites that actually run.
