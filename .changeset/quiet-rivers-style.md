---
"@input/pen-ai": patch
"@input/pen-react": patch
---

Document the HOST6 styling contract and route remaining taste colors through custom properties.

Suggestion underlines and the contextual-prompt selection overlay now read documented tokens with the previous literals as defaults, and `@input/pen-react` publishes `STYLING.md` as the workspace property reference.
