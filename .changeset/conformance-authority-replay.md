---
"@input/pen-conformance": patch
---

Add the recorded-trace authority replay for Wave 1 GATE 1.11. The host lock now rejects the old DOM-helper symbols on that filename, and the committed corpus is split/merge/remove with mapPoint algebra landings.
