---
"@input/pen-dom": patch
---

Land the caret on the new block after Enter on contenteditable engines so WebKit and Firefox keep authority (and then native selection) off the block that was just split.
