---
"@input/pen-ai": patch
---

Advertise a route-appropriate tool set on `tool-edit` passes instead of the full granted document toolset (EC16). An annotated working set offers `edit_document` alone; a pass without block ids keeps the discovery reads needed to find a target. Tool allowlisting and authority are unchanged.
