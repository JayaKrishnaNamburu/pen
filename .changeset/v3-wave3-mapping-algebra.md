---
"@input/pen-core": minor
"@input/pen-types": minor
---

Delete the change-summary mapping algebra and ring buffer. Selection AS3 fallbacks retarget from structural recipes and live document geometry; each CRDT event gets its own summary instead of a reused `latest()`.
