---
"@input/pen-core": patch
---

Put RTL, bidi marks, isolates, and multi-unit graphemes in the change-summary property fuzz, hash hyphenated nightly seeds so nights are not all seed 0, and assert that collapsed `mapRange` stays collapsed.
