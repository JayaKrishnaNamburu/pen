---
"@input/pen-ai": patch
---

Show streamed edit preview text as soon as it arrives. One stable span grows in place (no fill-mode fade), incomplete list markers stay off-screen, and the browser paints at most once per frame so a burst of fragments does not chop.
