---
"@input/pen-dom": patch
---

Add a standalone overlay paint-plan contract in pen-dom.

`createOverlayLayer` exposes the OV1–OV3 read/write paint-plan API without wiring multiplayer, AI, or React overlays.
