import type { Decoration, DecorationSet } from "@pen/types";

export function createDecorationSet(
  _decorations: Decoration[],
): DecorationSet {
  throw new Error(
    "Not implemented — createDecorationSet is available in Wave 3.",
  );
}

export function emptyDecorationSet(): DecorationSet {
  throw new Error(
    "Not implemented — emptyDecorationSet is available in Wave 3.",
  );
}
