import type { CreateEditorOptions, Editor } from "@pen/types";

export function createEditor(_options?: CreateEditorOptions): Editor {
  throw new Error("Not implemented — createEditor is available in Wave 3.");
}
