# @pen/export-xml

XML exporter and importer for Pen documents.

## Install

```bash
pnpm add @pen/export-xml
```

## What It Provides

- `xmlExporter` for XML serialization
- `xmlImporter` for importing Pen XML documents

Use this package when you need an interoperability-oriented format layered on top of Pen's JSON document model.
