# @input/pen-eslint-plugin

Pen's custom ESLint rules. Private to the workspace; consumed by the root `eslint.config.mjs`.

This package is plain ESM JavaScript with no build step so ESLint can load it directly. Relative imports therefore carry `.js` extensions, which Node ESM requires — this is the documented exception to the extensionless-import convention in `.cursor/rules/pen-import-path-conventions.mdc`, not a mistake to fix.

## Rules

| Rule                                     | Spec rule | Notes                                                                                                                                                                                                                                                                                                          |
| ---------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `no-html-injection-sinks`                | SEC2      |                                                                                                                                                                                                                                                                                                                |
| `no-unescaped-markup-concat`             | SEC5      | Export packages, schema `toHTML`, and clipboard HTML. Needs an adjacent comment naming SEC5 / already-serialized / clamped / justified.                                                                                                                                                                         |
| `no-above-floor-api`                     | HOST4     | Allowlist: `scripts/above-floor-api-allowlist.json` (fallback + degradation required). A site is a path string or `{ path, reason }`; a path-only object is the parking-lot shape and does not silence the rule.                                                                                                 |
| `no-bare-random-uuid`                    | HOST4     |                                                                                                                                                                                                                                                                                                                |
| `no-framework-free-modules-in-renderers` | API6      | Re-export stubs stay. Allowlist: `scripts/renderer-framework-free-allowlist.json`.                                                                                                                                                                                                                             |
| `no-module-scope-browser-globals`        | HOST2     |                                                                                                                                                                                                                                                                                                                |
| `no-user-facing-literals`                | LOC1      | Allowlist: `eslint-disable-next-line` with a reason.                                                                                                                                                                                                                                                           |
| `no-ascii-word-boundaries`               | LOC4      | `textSegmentation.ts` is the HOST4 fallback allowlist.                                                                                                                                                                                                                                                         |
| `no-bare-case-folding`                   | LOC5      | Identifier folds and display casing are allowlisted in-rule.                                                                                                                                                                                                                                                   |
| `no-implicit-locale`                     | LOC3      | Tests, playground, docs, and root `scripts/` are allowlisted.                                                                                                                                                                                                                                                   |
| `no-aria-hidden-visible`                 | AX4, AX7  | Overlay / focus-sink / decorative sites need an adjacent comment naming AX7, "focus sink", or "Justified".                                                                                                                                                                                                      |
| `no-unstyled-focus`                      | AX5       | `outline: none` is allowed only with a nearby `:focus-visible` ring.                                                                                                                                                                                                                                            |
| `no-v1-extension-fields`                 | facets    | Flags `Extension.keyBindings` / `inputRules` / `decorations` declarations. Schema `keyBindings` and local `const` names stay quiet.                                                                                                                                                                             |
| `no-selection-timers`                    | S4        | Scope is the in-config `modules` list (focus, offsetDomain, caretPositions, v1 backend/IME offenders) plus a basename-contains-`selection` fail-closed net so `selectionReader.ts` cannot silently escape. `outOfScope` names non-selection files; that is not a waiver. Allowlist: `src/rules/no-selection-timers-allowlist.json`. |
| `no-unscheduled-measure`                 | SCH1      | Geometry reads in `packages/rendering` runtime. Allowlist: `scripts/unscheduled-measure-allowlist.json` (file + symbol + reason).                                                                                                                                                                                                  |
| `no-bidi-override`                       | RI1       | `bidi-override` in renderer style. Allowlist: `scripts/bidi-override-allowlist.json`.                                                                                                                                                                                                                                               |
| `no-json-stringify-signatures`           | SCALE2    | `JSON.stringify` in core and rendering runtime. Allowlist: `scripts/json-stringify-allowlist.json`.                                                                                                                                                                                                                                 |
| `no-selection-state-properties`          | selection | `selection` / `sel` / `nextSelection` property reads of `isCollapsed` / `isMultiBlock` / `blockRange`. Allowlist: `scripts/selection-state-properties-allowlist.json`.                                                                                                                                                              |
| `no-pen-deep-imports`                    | API4      | `@input/pen-*` `/src/`, `/dist/`, and unpublished subpaths. Allowlist: `scripts/pen-deep-imports-allowlist.json`.                                                                                                                                                                                                                   |

Rules that need no custom code are configured directly in the root config: the dynamic-code ban (SEC8) uses core `no-eval` / `no-new-func` / `no-implied-eval`.
