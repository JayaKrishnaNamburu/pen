// I15/GA1 red-proof: a marked ban-glob. Empty population is the intent.
export default [
	{
		files: [
			// I15-zero: ban-glob; deleted-satellite must not return
			"packages/deleted-satellite/src/**/*.ts",
		],
		rules: {},
	},
];
