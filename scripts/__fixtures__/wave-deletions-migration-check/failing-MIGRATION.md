# Host adoption guide (fixture)

## `retiredSymbol` is gone

Hosts must stop importing it.

The `undocumentedDeletion` symbol is mentioned only in this paragraph,
not in any heading — that must not count as documented.
