# Host adoption guide (fixture)

## `retiredSymbol` is gone

Hosts must stop importing it.

## Path `gone.ts` no longer ships

The file is deleted. A body-only mention of `otherToken` does not count.
