# coverage:rules claimed scope
# IDs in force: a test name must contain each ID. Each wave appends here.
# Unlisted spec IDs are reported, not failed, until their wave closes.
#
# Before adopting a family prefix, check spec/charter/rule-ids.md (RC2).
# That registry is the authoring gate. Do not invent a prefix that
# collides with a reserved or recorded family.
#
# Gate-enforced IDs live in scripts/gated-scope.txt, not here. A
# claimed-scope ID without a test name fails. Listing a docs/CI rule
# here would hide the gap without verifying the gate still runs.
#
# Wave 0: I2, I3, I10, I11 (I11 seam 1 half).
# Wave H: CH1–CH9 (CH5/CH7 were already greppable).
# Wave 1 (2026-08-22): AN1–AN3, AN5–AN9, AN11–AN14, AS1–AS5 — only
# IDs whose test names contain the token and whose assertions match
# the rule. Held back: AN4 (fuzz name is AN1-AN5, not AN4), AN10
# (cell addressing has no claiming test).
# Already greppable in this checkout: HOST4, CH7, DUR1–DUR5, SEC1–SEC9,
# IOP3, IOP5, CH5, R1–R5, R7, I8, BR1, BR2, G1, G2, G5, AIB5, AIB6, COL1.
# Wave P leftovers: API1, API2, API3, API5, API9.
# Wave P gated: API8 (positioning-check). Declined: API4 (one sibling
#   is not the boundary), API7 (size-limit held; provenance unexercised).
# Wave C leftovers: COL2–COL4, COL6.
# Wave T leftovers: IOP1, IOP2, IOP4, IOP6.
# Wave V leftovers: DUR6, DUR7.
# Wave F leftovers: SCALE1, SCALE3 (bench axes/baselines), SCALE4 (heap soak),
#   SCALE5 (windowed-host conformance scenario).
# Wave E leftovers: HOST1, HOST2, HOST5.
# Wave P leftovers, second pass: API6 (renderer framework-free lint).
# Lane 102 (2026-08-21): moved informational claims into force where the
# named test can fail the rule. Held back (do not treat as covered):
#   API10 — IOP4 ingest names, not the inert-surface inventory
#   BR3 — proves computeBidiRuns is stateless, not G2-cached
#   DIR2 — asserts props.direction / omit-on-auto, not resolved dir
#   M3  — asserts logical line edges; spec is visual
#   OV3 — DOM onPaintPlan only; React/Vue primitives not rebound
#   P2  — UAX#9 first-strong, not projector-on-divergence
#   R6  — compute receives editor; purity is untested
#   SCH1 — scheduler FIFO names, not no-unscheduled-measure
#           (gate-enforced in gated-scope.txt; keep out of this file)
# Lane 156 (2026-08-21): re-examined the nine claims-outside-scope IDs.
#   AIB2 appended — bounds tests fail if compaction/caps regress; type
#   gate (pen-stream-request-no-editor.mjs) is green and self-tests
#   nested + top-level editor. The other eight stay held back: each
#   claiming test names the ID but asserts a different property.
# Lane infra (2026-08-21): A1 holdback lifted — selectionAuthority tests
# now assert total validation (missing block rejected, empty-block clamp).
# Still held: P2 (UAX#9, not projector-on-divergence). Not claimed, no
# asserting test yet: C1–C4, O1–O5, S1.
#
# Lane bench/coverage (2026-08-23): re-measured C1–C4, O1–O5, P2 with
# coverage-rules test-name detection (positive control: K1, K4).
#   C1–C4 — no test name contains the token. IME suite not staffed
#     (spec-v2/03-selection.md §8; wave-05-selection 5.6). K4 covers
#     composition Escape in keymap.test.ts but is not named C1.
#   O1 — playground/e2e/specPortLiveTrace.spec.ts names O1 and asserts
#     the opposite (overlay shown for ordinary typing). Do not claim.
#   O2–O5 — no test name contains the token.
#   P2 — greppable, but the claiming names are UAX#9 first-strong
#     (direction.test.ts, bidi wave) or reader "diverge" / request
#     counts (selectionReader, unfocusedReconcileProjection), not
#     projector flush after a divergence report with unchanged version.
#
# spec-v3 OP / OPB / INT entered the inventory by derivation
# (2026-08-24), not by this file. GATE 0.1 reports them; it does
# not fail. Do not append the unclaimed ones here to dodge a red
# — that is someone else's claim work.
#
# That claim work landed later the same day, so the holdback above is
# discharged rather than dodged: INT1–INT3, OP1–OP5 and OPB1–OPB6 now
# have named tests, each with a recorded break-on-purpose failure.
# Appended below: INT1–INT3, OP1–OP5, OPB1–OPB4, OPB6 (13).
#
# HELD BACK — OPB5, and this one is a spec-vs-code gap rather than
# missing test work, so it must not be appended until the code moves.
# 03-ops.md:159 requires that a profile which denied `split-block`
# now denies `intent: "pen.splitBlock"`. `filterOpsForDocumentProfile`
# never receives origin, so denial by intent is not implemented at
# all. The OPB5-named test asserts something else that is true — a
# split whose `insert-block` is flow-disallowed is dropped — while a
# paragraph→paragraph split with that intent still applies. Same
# category as the BR3 / R6 holdbacks: the name is not the coverage.
#
# OPB2 is appended for its first half only ("one executor per
# primitive"), which the named test asserts and can fail. Its second
# half — Y-level wire shape unchanged between v2 and v3 — is carried
# by the op-equality corpus behind GATE 4.5, which does not name
# OPB2. That is the intended gate/claim split this file describes at
# the top: gate-enforced substance belongs in gated-scope.txt, not
# here. Do not "fix" it by renaming a corpus test to contain OPB2.
#
# Lane coverage-ratchet (2026-08-23): adjudicated the 18 claims-outside
# claimed-scope IDs. Appended only those whose assertion fails if the
# rule is violated: AN4, AN10, C2, C3, C4, DIR2, I4, M3, O2.
# Still held (name is not coverage):
#   API10 — IOP4 ingest + DUR6 describe; not the inert-surface inventory
#   B2 — EditContext typing landed; not textupdate → pen.insertText
#   BR3 — computeBidiRuns is stateless; spec is G2-cached with geometry
#   C1 — live CDP scenario is knownDefect / test.fail(); do not convert
#     an expected product failure into coverage. Synthetic Escape
#     scenario asserts never-preventDefault but is not real IME.
#   I6 — keymap catalog dispatches commands; file says key-press half
#     (rule I6) is outstanding
#   O1 — playground/e2e and O1-LIVE assert overlay for ordinary caret
#     (recorded divergence / opposite of O1's atom-adjacent-only list).
#     The atom-edge scenario is real; the claiming set is contaminated.
#   OV3 — DOM onPaintPlan callback only; React/Vue primitives not rebound
#   P2 — UAX#9 first-strong or reader "diverge", not projector flush
#   R6 — compute receives editor; purity (no apply / set / DOM) untested

# I2/I3 unclaimed 2026-08-23: v3 Wave 3 deleted the mapping algebra those
# invariants describe, along with changeSummaries.properties.test.ts.
# spec-v2/V3-BOUNDARY.md already records I2/I3 expansion as skipped for v3.

# EM4 unclaimed 2026-08-24: v3 Wave 6 deleted the stamp-2 remote heal it
# describes, along with emptyBlocks.em4.test.ts. The rule was Wave 5
# scaffolding for stamp-2 writers inside this tree and its removal is
# scheduled in spec-v3/waves/wave-6-surface-release.md. EM1-EM3 and EM5-EM8
# stay in force. Do not re-add a test named EM4 to close this line: there is
# no rule left for it to fail on.

# I11 unclaimed 2026-08-25: v3 retired I11 → I14. The two-seam confinement
# those tests named has no subject: the sentinel is gone from storage
# (spec-v3/04-empty-blocks.md; spec-v2/01-architecture.md I11 RETIRED
# 2026-08-24). I1, I4–I10, and I12–I15 stay in force. Do not re-add a
# test named I11 to close this line: there is no confinement left for
# it to fail on.

# SM1 + SM3 unclaimed 2026-08-25: v4 Wave 3 DL2 deleted their subject, and
# with it slotAdapter.sm1.test.ts, which was the sole claimant of all three
# SM ids. SM1 describes getSlot/setSlot as in-train adapters over facets;
# the accessors and slotAdapter.ts are gone. SM3's enforcement retired by
# design in the same PR — DL2 requires the no-new-slots gate and allowlist
# to go with the mechanism, since "a gate that polices a deleted mechanism
# is scaffolding squared" (spec-v4/04-scaffolding.md). SM2 stays in force:
# the documented accessors still exist and are facet reads now, claimed by
# packages/extensions/search/src/__tests__/extension.test.ts. Do not re-add
# tests named SM1 or SM3: there is no adapter and no slot lint left to fail.
I10
HOST4
HOST6
CH7
DUR1
DUR2
DUR3
DUR4
DUR5
SEC1
SEC2
SEC3
SEC4
SEC5
SEC6
SEC7
SEC8
SEC9
IOP3
IOP5
CH5
R1
R2
R3
R4
R5
R7
I8
BR1
BR2
G1
G2
G5
AIB5
AIB6
COL1
I1
SM2
I7
ST1
ST2
ST3
ST4
ST5
ST6
CH1
CH2
CH3
CH4
CH6
CH8
CH9
LOC1
LOC2
LOC3
LOC4
LOC5
LOC6
AX1
AX2
AX3
AX4
AX5
AX6
AX7
AX8
API1
API2
API3
API5
API9
COL2
COL3
COL4
COL6
IOP1
IOP2
IOP4
IOP6
DUR6
DUR7
SCALE1
SCALE3
SCALE4
SCALE5
HOST1
HOST2
HOST5
API6
A1
A2
A3
A4
A5
A6
AIB1
AIB2
AIB3
AIB4
B1
BR4
D1
D2
D3
D4
D5
DIR1
DIR3
G3
G4
I9
K1
K2
K4
M1
M2
M4
M5
M6
N1
N2
N3
OV1
OV2
P1
S2
S3
S4
S5
S6
SCALE2
SCH2
SCH3
T1
T2
T3
T4
T5
T6
AN1
AN2
AN3
AN5
AN6
AN7
AN8
AN9
AN11
AN12
AN13
AN14
AS1
AS2
AS3
AS4
AS5
AN4
AN10
C2
C3
C4
DIR2
I4
M3
O2
EM1
EM2
EM3
EM5
EM6
EM7
EM8
INT1
INT2
INT3
OP1
OP2
OP3
OP4
OP5
OPB1
OPB2
OPB3
OPB4
OPB6
