{
  "_comment": "RI1 (spec-v2/08-bidi.md): known bidi-override style sites in packages/rendering/{dom,react,vue} runtime src. Each entry needs file, a positive integer line, and a non-empty reason. unicode-bidi: isolate is allowed and is not a hit. Remove an entry in the same PR that deletes the override.",
  "entries": []
}
